version: '3.8'
services:
  efun_app-postgresql:
    image: postgres:13.2
    # volumes:
    #     - ~/volumes/jhipster/efun_app/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=sa
      - POSTGRES_PASSWORD=yourStrong(!)Password
      - POSTGRES_DB=efun_app
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5433:5432
